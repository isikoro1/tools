# マトリックスのあの画面ジェネレーター

Canvasでマトリックス風の文字雨を生成できる静的Webツールです。好きな文字列やプリセットを使って、画面表示、画像出力、動画素材化ができます。

## 主な機能

- 文字プリセット切り替え
- カスタム文字入力とカスタムプリセット保存
- 文字サイズ、文字列上限、方向、文字順、色の調整
- PNG出力
- GIF出力
- MP4/WebM動画出力
- JSON設定保存・読込
- フルスクリーン表示

## 使用技術

- HTML
- CSS
- Vanilla JavaScript
- Canvas API
- MediaRecorder API
- localStorage

## 使い方

1. `index.html` をブラウザで開く、または GitHub Pages の公開URLへアクセスします。
2. 設定パネルで文字、文字サイズ、文字列上限、方向、奥行き、色などを調整します。
3. 画面をクリックまたはタップすると設定パネルを非表示・再表示できます。
4. `PNG`、`GIF`、`MP4/WebM` ボタンから素材を書き出せます。
5. `JSON save` / `JSON load` で設定を保存・復元できます。

## 工夫した点

- 文字入力は改行ごとに別パターンとして扱い、複数の文字列の雨を生成できます。
- 半角英数字を全角化して、日本語文字と横位置が揃いやすいようにしています。
- 描画ループ中のDOM参照を減らすため、設定値をキャッシュしています。
- 文字の発光とぼかしは軽い固定値にし、黒背景に映える見た目と処理負荷のバランスを取っています。
- 速度、密度、残像、文字間隔、文字の太さ、奥行きは固定値にして、調整項目を整理しつつ安定した見た目にしています。
- 静的ページとして公開しやすいよう、外部script依存を追加せずに実装しています。

## 今後の改善候補

- `app.js` の責務分割
- JSON読込時の設定値バリデーションのスキーマ化
- GIF出力と画面表示用シミュレーション状態の分離
- 設定項目のメタデータ化
- テストしやすい純粋関数の切り出し

詳細な改善計画は [docs/refactoring-plan.md](./docs/refactoring-plan.md) にまとめています。
